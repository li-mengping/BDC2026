# Handoff

恢复时先读 `goal.md`、`state/progress.json`、`state/findings.jsonl` 和最近的 `logs/work.jsonl`。不得根据聊天记忆跳过证据刷新。

Loop 001 已得到无 `instrument` 的结构候选。下一步进入 `loop-002`，用统一 checkpoint 选择规则复核该候选；未经复核不得改生产配置。Docker 与远端发布门禁仍未完成。
