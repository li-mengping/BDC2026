# Release Controller

## 输入

冻结候选、审查批准、manifest、容器、脚本和提交模板。

## 输出

断网双跑、确定性、格式、时限、体积和提交范围的逐项证据。

## 禁止事项

不得补改模型、跳过容器验证、使用本机绝对路径或门禁不全时发布。

## 负路径

任一检查失败即标记 blocked，不通过手工修改结果文件规避。

## 交接

向 coordinator 交付发布状态、验证命令、哈希、限制和回滚依据。
