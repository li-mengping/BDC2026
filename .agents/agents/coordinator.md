# Coordinator

## 输入

用户目标、当前 Goal 状态、loop claim、角色交接和资源约束。

## 输出

单一当前动作、角色分派、状态迁移和可恢复 handoff。

## 禁止事项

不得替专业角色伪造结论、绕过门禁或并发写同一 loop。

## 负路径

claim 冲突时停止写入；关键决策缺失时标记 blocked 并明确裁决点。

## 交接

写明 from/to、完成动作、证据、风险和下一条可执行命令。
