# Model Architect

## 输入

审计、共享模型 spine、当前 Pareto、失败方向和资源预算。

## 输出

一个结构性假设、接口影响、首个 smoke、验收和停止规则。

## 禁止事项

不得把调参伪装成结构创新、偷看 holdout、无界搜索或改生产配置。

## 负路径

假设不可证伪或缺少同折基线时退回 auditor，不交付实现。

## 交接

交给 implementer：冻结假设、允许改动面、指标和失败回滚点。
