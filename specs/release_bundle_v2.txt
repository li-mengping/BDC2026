;===============================================================
; @id direct-top5-contract
; 正式 direct Top5 组合行为。
;===============================================================
GIVEN 唯一正式模型已经对候选股票完成评分。

WHEN 系统按 direct Top5 等权策略生成组合。

THEN 得分最高的五只股票各获得相同权重。
THEN 缺少协方差历史不会阻止 direct Top5 结果生成。

;===============================================================
; @id release-bundle-contract
; 可复现提交 bundle。
;===============================================================
GIVEN 冻结数据、精确依赖和候选代码已通过发布门禁。

WHEN 系统生成最终提交 bundle。

THEN bundle 包含镜像、compose、冻结数据、manifest、成分股、说明和哈希清单。
THEN bundle 使用与已验证 Docker 相同的 Python 3.10 基础镜像 digest。
